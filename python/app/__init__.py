# app/ — слой приложения: окно, экраны, отрисовка.
#
# Движок живёт в life/ и про экран не знает. Здесь наоборот: всё, что видит
# игрок. Модули settings, session, history и camera намеренно без pygame —
# их проверяют тесты без дисплея, — поэтому этот файл пуст и ничего не
# импортирует.
