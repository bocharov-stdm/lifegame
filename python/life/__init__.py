"""life — движок симуляции.

Здесь живут мир, сетка соседей, сущности и настройки. Про экран пакет не знает
ничего: pygame внутри `life/` не импортируется, поэтому движок одинаково
работает и в окне, и в тестах, и в headless-прогоне на 100k тиков.

Окно, экраны и отрисовка лежат в пакете `app/`, точка входа — `main.py`.
"""
