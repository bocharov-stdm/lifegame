"""Lifegame — эволюционная симуляция.

Модуль намеренно пустой: `tools/calibrate.py` подменяет значения в
`lifegame.config` до импорта симуляции, а модули читают их в момент импорта.
Любой импорт `world` или `render` отсюда молча сломал бы этот механизм —
переопределения перестали бы действовать, не выдав ошибки.
"""
